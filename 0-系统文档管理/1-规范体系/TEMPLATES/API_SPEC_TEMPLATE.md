# {系统名称} — 接口规格文档模板

> **版本**: vX.X | **更新日期**: YYYY-MM-DD
> **定位**: 子系统对外接口规格，对齐 [DOC_STANDARD.md](../../1-规范体系/DOC_STANDARD.md) §3.3

---

## 1. 接口概览

### 1.1 接口类型

| 类型 | 数量 | 说明 |
|------|------|------|
| HTTP API | N | 端口 XXXX |
| Python API | N | SDK 式调用 |
| CLI 命令 | N | 命令行入口 |

### 1.2 接口列表

| 接口 | 方法 | 路径/签名 | 说明 |
|------|------|-----------|------|
| 接口1 | GET | `/api/xxx` | 描述 |

---

## 2. 认证方式

| 方式 | 说明 |
|------|------|
| 无需认证 | SDK 集成 |
| API Key | HTTP Header: `Authorization: Bearer xxx` |
| Token | 描述 |

---

## 3. 接口详情

### 3.1 {接口名}

**方法**: GET/POST
**路径**: `/api/xxx`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `field1` | string | 是 | 描述 |

**请求示例**：
```json
{
  "field1": "value"
}
```

**响应参数**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `field1` | string | 描述 |

**响应示例**：
```json
{
  "field1": "value"
}
```

---

## 4. 错误码

| 错误码 | HTTP 状态 | 说明 |
|--------|-----------|------|
| `SUCCESS` | 200 | 成功 |
| `INVALID_PARAM` | 400 | 参数错误 |
| `UNAUTHORIZED` | 401 | 未授权 |
| `NOT_FOUND` | 404 | 资源不存在 |
| `INTERNAL_ERROR` | 500 | 内部错误 |

---

## 5. 版本管理

### 5.1 版本策略

- 接口版本通过 URL 路径区分（如 `/api/v1/xxx`）
- 向后兼容：新增字段不破坏旧版本

### 5.2 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | YYYY-MM-DD | 初始版本 |

---

**文档版本**: vX.X
**最后更新**: YYYY-MM-DD
