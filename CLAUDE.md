# CLAUDE.md — 认知系统协作引导

## ⚠️ 启动必读（每个任务开始前必须执行）— 硬约束

**在开始任何编码/调试/分析任务前，必须先调用 `recall` 工具检索相关经验。这是硬约束，不可跳过：**

```
recall(context="当前任务描述或问题关键词", top_k=5, min_quality="C")
```

**违反此约束等同于放弃"越用越聪明"能力**。recall 是免费的（只读不写），但能避免重复踩坑、复用已有解决方案。
即使你认为没有相关经验，也必须调用一次——记忆库可能有你不知道的相关记录。
`min_quality="C"` 确保返回最多结果，检索到的记忆按相关度排序。

> **为什么必须用 C 级？** 当前记忆库 98% 是 C 级记忆，用 B 级阈值会返回空结果。
> C 级记忆虽未验证，但包含有价值线索。按 score 排序优先看高分记忆。

---

本项目已接入**认知记忆系统**（cognitive memory system），具备"越用越聪明"能力。
通过 `recall`（检索经验）→ 编码 → `record`（记录新经验）→ `verify`（验证升级）形成认知闭环。

## 🔧 可用的认知 MCP 工具

认知系统通过 MCP server 暴露 5 个工具，IDE 可直接调用：

| 工具 | 何时调用 | 作用 |
|------|---------|------|
| `recall` | **任务开始前**（硬约束，不可跳过） | 检索相关历史经验，避免重复踩坑 |
| `record` | 发现新经验/反模式时 | 将经验写入记忆系统 |
| `verify` | 实践验证后（成功或失败） | 贝叶斯置信度更新，触发蒸馏 |
| `stats` | 需要查看系统状态时 | 记忆统计 |
| `health` | 排查问题时 | 健康检查 |

## 📋 标准协作流程

### 1. 任务开始前 — `recall`（硬约束，不可跳过）
```
recall(context="当前任务描述或问题", top_k=5, min_quality="C")
```
检索到的经验会帮助你避免重复踩坑，复用已有解决方案。
**即使没有检索到结果，也表明你已检查过记忆库——这是闭环的起点。**
**跳过此步骤会导致重复踩坑、忽视已有解决方案，是严重的协作违规。**

### 2. 任务完成后 — `record` + `verify`
如果发现了新经验、反模式或解决方案：
```
record(content="经验内容", quality_level="B", tags=["标签1","标签2"])
```
如果验证了某条已有记忆（成功或失败）：
```
verify(memory_id="VM-xxx", success=true)
```
**verify 会触发贝叶斯置信度更新和动态蒸馏**，是"越用越聪明"的关键。

## 🎯 质量等级说明

| 等级 | 含义 | 置信度范围 |
|------|------|-----------|
| S | 公理级，多次验证 | ≥0.95 |
| A | 可信级，实践验证 | ≥0.70 |
| B | 待验证级 | ≥0.40 |
| C | 假设级 | ≥0.20 |
| D | 已证伪 | <0.20 |

## 📁 系统位置

- 核心代码：`4-MEMORY/9-工具与接口/`
- 记忆数据库：`4-MEMORY/data/cognitive_memory.db`
- 蒸馏产物：`4-MEMORY/2-交易记忆单元/bayesian_memories.json`
- 会话数据：`.cognitive/sessions/`

## ⚠️ 注意事项

1. **recall 是免费的** — 检索不会修改任何数据，放心调用
2. **recall 是必须的** — 任务前不调用recall，等于放弃"越用越聪明"能力
3. **verify 很重要** — 只有 verify 才能让记忆"升级"，从假设变为可信
4. **tags 要准确** — 好的标签能提升后续 recall 的命中率
5. **不要记录噪音** — 交易系统自动产生的文件变更不需要 record
