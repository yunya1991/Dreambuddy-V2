# MU-DEV-CORE — 开发记忆单元核心

> 单元: MU-DEV | 容量: ≤ 2,000 字符 | 上次压缩: 2026-07-27 | 当前占用: ~85%
> 记忆类型: Procedural (程序记忆) + Semantic (语义记忆)

## 工程原则（S级 — 程序记忆）

- P1 工业级标准：代码有规范、文档有模板、流程有标准
- P2 彻底解决：充分调研（A1+矛盾论）再动手，做彻底
- P3 理论实践一致：文档和代码必须对齐，用A8校验法检测不一致
- P4 记忆驱动：建立记忆系统让管理阶梯式积累
- P5 文档先于代码：先形成技术文档，再开始落地实现

## 方法论要点（A级 — 程序记忆）

- 矛盾论：抓主要矛盾，次要矛盾让步；当前主要矛盾=文档代码不一致
- A1调研法：动手前充分调研（文档→代码→对比），产出可验证断言
- A8校验法：理论清单→实践清单→逐条对比→根因分析→修复→验证
- 接口语义隔离：统一接口filters字段由各应用记忆自行定义

## 关键架构决策（A级 — 语义记忆）

- 双层架构：总记忆（索引+通用经验）+ 应用记忆（子系统专属）
- 适配器模式：L4记忆统一接口封装，不修改现有代码
- 记忆ID规范：AM-{SYSTEM}-{TYPE}-{RAW_ID}
- 三级容量：Tier0核心(≤8K) → Tier1扩展(≤50K) → Tier2归档(不限)
- 80%压缩触发：容量达80%时consolidation，压缩表达不删内容

## 开发经验 Top 5（A/B级 — 情景→语义）

1. subprocess+shlex.split在设置环境变量时会失败，用shell=True
2. launchd任务label要从安装脚本查，不能猜
3. 改动之前先调研，A1调研法是标准流程
4. 运行时缺陷优先级高于文档债
5. inspect.py与Python标准库同名会导致循环导入，用懒加载规避

## 当前技术债焦点

- D048/D049已修复（16-调控系统函数不存在+SKILL名不一致）
- 下一步：记忆单元分区实施 + 自动更新机制

## A8 校验状态（自动更新）

- **子系统**: 16-调控系统
- **最后校验**: 2026-07-27 10:51
- **一致性得分**: 23.684210526315788%
- **待处理项**: 15 项
- **重点问题**:
  - [P1] NOTIFY: 确认代码中是否已删除/改名，如已删除则从 API_SPEC 中移除
  - [P1] _calc_simple_technical_signals: 确认代码中是否已删除/改名，如已删除则从 API_SPEC 中移除
  - [P1] a9_exit_decision.evaluate_position_for_exit: 确认代码中是否已删除/改名，如已删除则从 API_SPEC 中移除
  - [P1] exit_executor._get_okx_client: 确认代码中是否已删除/改名，如已删除则从 API_SPEC 中移除
  - [P1] exit_executor.create_executor_from_env: 确认代码中是否已删除/改名，如已删除则从 API_SPEC 中移除
